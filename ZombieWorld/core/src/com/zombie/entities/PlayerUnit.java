package com.zombie.entities;

import com.zombie.gfx.TextureManager;

public class PlayerUnit extends GameUnit {
    public PlayerUnit(TextureManager.TEXTURE texture) {
        super(texture);
        boolean inSquad = false;
    }
}

